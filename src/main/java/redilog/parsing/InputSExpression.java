package redilog.parsing;

import net.minecraft.util.dynamic.Range;

public class InputSExpression extends SymbolExpression {
    public InputSExpression(Range<Integer> range) {
        super(range);
    }
}